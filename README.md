# Install

## Global Node Modules

Install brocolli-cli and bower globally

```
$> npm install -g brocolli-cli
$> npm install -g bower
```

## Node Modules

Install node module dependencies

```
$> npm install
```

# Build
